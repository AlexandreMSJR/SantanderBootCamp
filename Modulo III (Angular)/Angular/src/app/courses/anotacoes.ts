/*  {{ title }} <!in– Interpolação, quando queremos apenas exibir  –>

<br/>

{{ name }}

<br/>

<input [(ngModel)] = "name" name ='name' > 
<!in– one way data base ( quando utilizar apenas o {} two way { com chaves e  ( parenteses )}) –>
<!in– one way data base = quando queremos apenas ixibir o valor da propriedade –>
<!in– two way data base = tanto quanto exibir quanto alterar a propriedade –>


<td><img [src]="course.imageUrl"></td> <! precisa colocar o src em [PARA MESCLAR AS INFORMAÇÕES] >
    styleUrls: ['./star.component.css'] - precisamos passar dentro de um Array, pois podemos passar mais de um modelo de styles
*/